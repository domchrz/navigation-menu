import { StyledSection } from '../../styles';
import ProductList from './ProductList';

export default function Products() {
  return (
    <StyledSection>
      <ProductList />
    </StyledSection>
  );
}
